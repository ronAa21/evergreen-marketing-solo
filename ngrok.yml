version: "2"
authtoken: # Add your ngrok authtoken here (get it from https://dashboard.ngrok.com/get-started/your-authtoken)

tunnels:
  evergreen:
    proto: http
    addr: 80
    bind_tls: true
    inspect: true
    hostname: # Optional: Add your custom domain here if you have one
    # Example: hostname: your-domain.ngrok.io



